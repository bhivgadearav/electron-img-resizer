{
  "$schema": "./schema.json",
  "name": "image-app",
  "productName": "Image App",
  "version": "1.0.0",
  "description": "image resizing app using electron js",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx electronmon ."
  },
  "author": "Arav Bhivgade",
  "license": "ISC",
  "devDependencies": {
    "electron": "^20.2.0"
  },
  "dependencies": {
    "resize-img": "^2.0.0",
    "toastify-js": "^1.12.0"
  }
}
